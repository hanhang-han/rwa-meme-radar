# 按原始需求重构产品与数据流程

日期：2026-09-10。状态：设计方案，未实施；以《整体思路的考虑和描述(3).docx》为需求基准。文中阈值、六类关系的具体定义和工程安排属于建议，不冒充原文要求。

## 1. 产品目标与当前差距

目标：以统一的多链、多提供方数据模型发现、验证、解释 Meme 与股票代币之间的关系。X Layer / OKX 是已接入来源之一；链与提供方属于记录的可筛选属性，不设产品“主场”。

产品的基本对象是“某链上的 Meme 合约，与某个已识别股票代币之间的关系及其证据”。名字相同、价格上涨、交易频繁，都不能单独证明经济联系。

本次读取线上 API 时：OKX 状态为 unconfigured；展示 100 个最新新币、37 条名称候选，其中已验证直接配对为 0；五个行业篮子均无已验证成分。这是当时快照，不是长期统计，也不代表链上没有真实关系。

已核对的实现问题：

- 新币工厂是主要入口；股票名称匹配之后才查询配对池，漏掉不含股票关键词的 Meme。
- 每次从滚动创建窗口重建代币交易统计；窗口外旧币的新交易没有进入持续资产账本。
- 资产归档上限 2,000，候选上限 400；API 另有展示截断。缓存与快照不能替代长期索引。
- 当前保存最大成交额、覆盖买卖次数的方式不能成为可对账的累计口径。
- 前端仍有旧版发行热度，与原文的成交量增速、持有人增速、股票锁仓热度不是同一指标。
- 篮子以保留窗口最早共同样本重建基期，成员/窗口变化可能改变历史可比性。
- X Layer 发现、验证和 OKX 真实联调仍不完整。增加 OKX 跳转链接不能替代业务接入。

## 2. 需求与页面对应

| 原文要求 | 产品落点 | 完成条件 |
|---|---|---|
| 发现、验证、解读 | Live → Radar → Detail | 每条信号能定位资产、证据、数据时间和结论 |
| 四个核心页面 | Live / Meme / Stock / Detail | 各页面回答不同问题，列表进入稳定详情地址 |
| 六种关系类型 | 关系标签和证据面板 | 每个标签独立核验，允许多标签并存 |
| 股票背景与联动 AI 卡 | Stock 侧栏及 Detail | 说明有来源，结论能回指证据，区分事实与推断 |
| Stock Flow 与热度 | Live 摘要及趋势 | 公式、范围、时间窗、缺项公开，不能用旧指标替名 |
| 行业篮子打样 | Live 图表及成分展开 | 五行业目录中至少一个满足真实成分及连续历史要求 |
| Pearson 联动与捕获率 | Detail | 对齐样本、价格来源与分母覆盖可核查 |
| 基础真实性/欺诈判断 | Detail 风险面板 | 明确检测范围、证据和未覆盖项 |
| 多链、多提供方统一接入 | 全局链/来源筛选、来源及跳转 | 每条资产、关系、行情与证据都有 provider、chainId、CA、时间和覆盖范围；只在口径一致时跨链汇总 |
| 聪明钱、捆绑、订单流 | 后续路线图 | 不挤占当前基础闭环建设 |

## 3. 发现与验证逻辑

### 三路发现，共用一个验证队列

1. 股票资产反查：由 OKX 与可核实发行方目录确定股票代币 CA → 找到这些 CA 的池子 → 识别另一侧资产。排除稳定币、基础币和另一只股票代币后，进一步确认对方是否为 Meme；未知资产标为待分类，不能直接当 Meme。
2. 机制证据发现：从项目可核验披露/合约发现股票金库、回购、兑换机制，核实地址归属和交易。
3. 新币与叙事发现：新币源、名称、公开叙事产生弱候选，用于补充前两路，不成为验证的先决条件。

直接池查询范围必须记录链、DEX、池版本、区块高度。范围内没找到不等于全链不存在。先发现池不等于确认有有效流动性。

### 三个独立维度

- 关系证据：未核验 / 核验中 / 已有证据 / 限定范围未发现 / 证据失效；单次请求失败为数据状态，不能判定关系不存在。
- 交易活动：尚无成交 / 低活跃 / 活跃 / 休眠；仅在覆盖完整时才能判断零成交。
- 安全与数据质量：风险命中、检测范围、来源、时效、完整性；已配对不等于安全。

不要把这些维度合成一个含义不明的“可信分”。

### 六类关系的建议定义

原文要求六类但没有逐项定义；沿用已有草案并补齐核验条件：

| 类型 | 证明条件 | 不能推出的结论 |
|---|---|---|
| 名称/叙事 | 名称命中或可引用叙事文本 | 官方关系、资产持仓 |
| 直接配对 | 同链池合约与两侧 CA 验证，股票侧来自已核实目录 | 流动性充分、合约安全 |
| 金库持仓 | 可归属项目的金库地址、目标股票代币余额及区块 | 余额永久锁定 |
| 回购联系 | 可追踪资金路径、回购交易、机制与执行主体 | 未来必然继续回购 |
| 兑换/权益 | 可核验条款、合约、兑换规则及可执行条件 | 所有法律权利均得到保证 |
| 统计联动 | 独立行情、对齐样本与可复算统计 | 因果、资金往来 |

每一对关系允许多种类型同时存在；财务关系、统计关系和名称线索分开显示。

### 同名与低质量活动

- 资产唯一键使用 chain + CA，不能按名称合并资产、交易和余额；地址规范化按链规则执行。
- 同名仅用于展示分组。保留原始名称；标准化大小写、空格用于聚合，避免激进删除字符导致不同名字误合并。
- 同名组显示合约数、已验证关系数、活跃数；展开后每个 CA 都可独立查看。代表项按证据、数据质量、有效流动性与活跃度排序。
- 短代码和常用词只记弱匹配，不自动认定对应某只股票；模糊匹配允许多个待核对标的。
- 零交易名称候选保存至原始流，默认不占据首页。发现实质证据时，无论交易量大小均进入验证。
- 微额交易如实计入链上原始买卖；降权只影响排序，不静默删除或改写成交统计。
- 活跃门槛按链、发行阶段配置并用历史分布校准，不能仅用固定 BNB 门槛套所有链，也不能仅凭一笔交易认定有效。

## 4. 四个页面

### Live Radar：今天出现了什么值得核实的变化？

推荐布局：

```text
品牌 / 全局 CA 或股票搜索 / 链与数据来源筛选 / 数据时效
股票热度与 Meme 热度概览 | Stock Flow | 今日新增已验证关系
热度趋势                              | 行业篮子趋势
最新关系事件                          | 最值得核实的候选
覆盖范围、统计口径、可展开的数据状态
```

- “最新线索”改为关系事件流：新配对、首次验证、金库变化、回购执行、证据失效等。展示对象、股票、事件、金额/规模、时间、证据入口。
- 同一关系轮询到相同事实不重复生成新事件；意义不同的事件保留时间线，批量事件可折叠。
- 弱名称候选另设小区块，首页可折叠；全链新币发行流移到 Meme Radar。
- 热度和篮子承担主视觉；Stock Flow 为独立摘要及可展开趋势，避免与锁仓总量混用。
- 数据尚未就绪时保留紧凑指标状态，列出缺少的输入和已完成覆盖，不能画无依据曲线。
- X Layer、BNB、Robinhood 等来源使用同一对象结构展示；每条指标都标明来源、链、时间与覆盖范围。缺少某来源的数据时显示其缺口，不以另一条链的数据补成同一条记录。

### Meme Radar：哪些 Meme 与股票可能有关系？

默认范围为已验证关系与具有可核查证据的待核验对象。使用筛选项：已验证 / 待核验 / 名称候选 / 全部新币 / 历史。

主表列：Meme 名称与 CA、网络、关联股票、关系类型与状态、24h 成交额、24h 买/卖、流动性或发行阶段、最后活动、数据状态。

支持股票/CA 搜索、链、关系类型、活动、风险筛选；同名组展开。创建时间作为辅助列，不能成为全站唯一排序依据。没有完整 24h 历史时注明实际覆盖范围，不把启动后的统计冒充 24h 完整值。

### Stock Radar：哪只股票正在吸引 Meme 活动？

以真实股票标的为主行，以交易所/标的标识确定身份；发行方、链和股票代币 CA 为子层。同一股票多个发行方不能算多只股票。

主表列：股票与行业、股票价格及交易时段、股票代币价格/溢价、已验证关联 Meme 数、活跃关联 Meme 数、配对池股票侧流动性、关联成交量、股票热度。

点击股票打开页内侧栏：AI 业务背景、发行方与各链 CA、相关 Meme 按关系类型分组、趋势、证据。名称候选数量单列，不与已验证数相加。

股票代币价格与原股价格比较需检查兑换比例、拆股/分红调整、币种及时间，不能默认一枚代币始终等于一股。

### Meme Detail：这个关系是否真实，资金规模多大？

按“结论 → 证据 → 活动 → 解读”排序：

1. 顶部：名称、网络、完整可复制 CA、创建/首次发现时间、最后活动、价格及关键交易指标。
2. 核心结论：与哪些股票存在何种关系；只在限定范围未发现证据时明确范围与时间。
3. 关系证据卡：池双方 CA、有效流动性、金库归属与余额、回购交易、兑换机制；支持查看交易/地址与复核时间。
4. 价格与成交量图、最近成交表；在独立价格与足够样本存在时展示标准化联动图、Pearson 与捕获率。
5. AI 股票背景、关系解读、风险与待验证问题，每项结论附可追踪来源。
6. 发现、验证、机制变化和失效的完整时间线。

按数据状态适配：

- 仅创建无成交：显示已确认创建信息、观察时段、暂无成交的依据和验证进度；不显示一屏空 K 线。
- 已有交易但无关系证据：展示真实成交与检索结果，关系结论仍然为待核实。
- 有关系证据但无充分价格：优先呈现证据，指标显示具体缺项。
- 历史资产：保留详情，明确最后样本时间与当前追踪状态。

保留原文四页面。Detail 是上下文页面，顶栏可保留“当前详情”入口；未选择资产时展示 CA 搜索/最近查看，避免点击第四个 Tab 得到空白。

## 5. 指标定义与上线条件

### 热度

原文规定成交量增速、持有人增速、股票锁仓价值三个信号做线性加权，没有规定权重。建议暂沿用现有 40/30/30 作为可版本化草案，三个输入均需归一化和公开定义。

- Meme 热度：该 Meme 的活动增速及与其可核验关联的股票资产规模。
- 股票热度：该股票所关联 Meme 群体的活动、持有人增长和股票资产规模；对同一地址/资产流向去重，不能简单累加各 Meme 分数。
- 必须区分实际锁定资产、可自由转出的金库持仓、配对池储备；它们不能都写成“锁仓”。
- 缺项不按零代入；原文完整热度未具备时可另显示“交易活跃度”，名称与公式须独立。
- 首页综合趋势使用明确的成分、权重与版本，记录覆盖率，避免成分变化伪装成热度上涨。

### Stock Flow

原文没有给出精确定义。建议首版限定为“已验证 Meme–股票配对池中，股票侧 LP 净注入”：加池股票数量减撤池股票数量，按事件时价格折算。图表分列流入、流出、净额。

交易造成的储备变化、金库转账、回购支出分开呈现；这个指标不叫投资者净买入。缺少 LP 事件或只有余额快照时不能计算成 Flow。

### 行业篮子

- 目录沿用原文：芯片、交易所、支付、托管、加密国库；分类依据与版本要可追溯。原文提及的币安研究报告未附全文，当前词典不可宣称已严格复现其分类。
- 首版按原文“股票配对 Meme”要求，仅纳入地址验证通过且有有效流动性与可靠价格的直接配对成分。
- 建议优先完成一个真实篮子，其余展示成分收集进度。至少三个合格成分作为发布篮子的初始产品门槛；少于三个称为单资产/成分观察，阈值允许评估调整。
- 固定基期 100，以可核实流通市值为权重；只有 totalSupply 时明确称“总供应估值加权试验指数”，不能当流通市值。
- 固定初始成分、定期调仓，用除数调整保持连续性，保存版本；不随页面可见列表或滚动窗口重新设基期。
- 处理供应变更、无效价格、暂停和退出；缺失成分不能被静默移除后继续输出相同口径指数。
- 展示指数、涨跌、成分数、权重、覆盖率、基期、调仓时间；区分不同网络范围。
- 不跨链简单合并资产；多股票关联成分必须有明确归属或分摊规则，不能在同一篮子重复计权。

### 统计联动

原文混用了 Pearson 和 beta，两者应纠正：Pearson 是相关系数，beta 是回归斜率。

首版保留原文价格 Pearson，明确称“价格水平相关”，提供窗口、共同样本数、来源、更新时间；建议补充收益率 Pearson 作为稳健性检查，不能把新增口径冒充原文。

价格必须独立获取，不得通过同一个 Meme–股票池的股票报价反推 Meme 美元价后制造机械相关。股票休市、序列恒定、价格过期、样本不足时不输出可信联动结论；不能无限前向填充。相关性也可以用于待验证候选，但标签始终是统计关系。

### 股票捕获率

同一链、同一已覆盖池集合、同一时点：Meme 配对池中的股票侧美元流动性 / 所有已覆盖股票交易池中的股票侧美元流动性。

分子是分母的子集。只覆盖部分 DEX 时称“已覆盖池捕获率”，公开范围，不能宣称全链比例。成交量占比和流动性占比是两个不同指标，不可互换。

## 6. 数据处理

### 持久化与统计

- 资产、股票标的、股票代币、池、原始交易事件、关系、证据、信号、行情样本分别存储；内存只做缓存。当前单服务器可用事务型数据库起步。
- 链上原始事件按 chain + txHash + logIndex 去重，记录 blockHash；同一交易多笔 Swap 保留事件，交易笔数另按 txHash 明确聚合口径。
- 持久化扫描游标，完成一个区间的事务后推进。重启从游标续扫，失败重试并记录缺口；链重组回滚受影响区间再重算。
- 按代币聚合所有后续事件，不要求它的创建事件仍在本次窗口。未见创建事件的资产可先建立待补元数据记录。
- 从逐笔事件计算 5m/1h/24h/观察以来买卖、成交量与独立地址；累计值不能来自滚动快照取最大值。
- 毕业/迁移后切换至 DEX 池追踪，保留同一资产身份；代币升级/新 CA 建立显式迁移关系。
- 原始数量使用整数及资产 decimals，交易按事件时汇率折算；只有当前汇率时标明当前估值，不能称历史成交美元额。
- 用经过核对的 ABI、事件样例及实际区块时间验收解码。估计时间必须标注，不能悄悄用于精确时窗统计。
- 展示 100 条/分页不影响保存历史。历史冷数据可以归档，保留资产、证据、信号与汇总索引；禁止按第 401 条候选之类规则无提示删除。
- 历史详情从持久索引加载，补扫通过有状态任务执行；不用有限最近区块查询当完整历史搜索。

### 数据状态

每项数据带 value、source、observedAt、window、coverage、status、reason。

| 状态 | 展示 |
|---|---|
| 在完整观察范围确认零 | 0，并明确时段 |
| 已有部分采样 | 当前值 + 覆盖时段/不完整标记 |
| 未采集或数据源未接入 | 待采集 / 来源未接入 |
| 更新失败、有历史值 | 上次值 + 过期时间 |
| 不适用或无法定义 | 不适用 + 原因，例如价格恒定 |

### 更新建议

以下为目标节奏，需以额度、实际延迟与源支持验收，不承诺当前已经做到：

- 活跃资产交易/新池：支持流式时使用事件订阅，否则增量轮询目标 10–30 秒；前端可见更新目标 15–30 秒。
- 候选证据：新事件触发，优先强证据，后台公平队列复核，防止固定列表前 80 条占满采样。
- 活跃行情：30–60 秒；指标以统一 5 分钟桶计算；财务/持有人输入按数据源可用频率独立标注。
- 休眠资产低频复核、发现新活动后提升优先级；重要关系持续追踪。
- 股票背景按日或资料变化更新，联动叙事按证据/指标实质变化更新。
- 接口失败显示最后成功时间，监控游标滞后、未补缺口、重试队列与数据过期率。

## 7. AI、风险及数据适配

AI 只消费结构化事实与可引用材料。输出“事实、解释、尚未证明的部分”，保存输入证据版本、模型和生成时间。已有规则说明继续标为规则，不能直接改名 AI。

股票背景包括公司业务、行业及股票代币发行方差异；关系叙事说明发现了什么、由哪些交易/余额证明、规模如何、哪些指标支持或不足。

当前阶段基础风险至少覆盖可取得的合约身份、权限/升级能力、已知地址风险和交易限制检测结果；明确检测时间与范围。高级聪明钱/捆绑/完整订单流按原文放后续。

适配器可从不同目录、报价或链上索引读取股票代币。它们必须归一为同一资产、合约、行情、池、成交和证据模型；页面按股票和关系聚合，不以链或提供方分栏。内部仍保留可追溯的原始出处、链 ID、合约和更新时间。

官方依据：https://web3.okx.com/zh-hant/onchainos/dev-docs/market/market-rwa-token

跳转优先使用已确认支持对应链/CA 的 OKX 产品入口；不能伪造交易深链。来源引用仍保留原始出处。OKX AI Provider 是可评估接入方向，不能把未接入描述为已集成。

## 8. 实施顺序与验收

1. 数据地基与首个来源可用性并行推进：核实 OKX/X Layer 数据、建立持久事件索引、准确解码和断点续扫；统一所有来源的资产、部署、池、交易、证据与行情模型。
2. 跑通至少一个真实关系闭环：从股票 CA 反查池，确认另一侧资产，生成证据与信号；为 AI 和指标准备真实样本。
3. 重构四页面：事件首页、Meme 筛选、股票聚合侧栏、证据优先详情、同名折叠和历史查询。
4. 逐项上线可计算指标和来源驱动 AI：先完成真实篮子打样，其他指标按数据覆盖验收；没有输入的指标不能用演示数据混充。

验收用例：

- 同名五个 CA：一个可展开组，五份独立事实；任何新证据能独立出现。
- 名字不含股票关键词但真实配对：能够发现并进入验证。
- 零交易但有真实池/金库证据：不会被活动门槛删除。
- 零交易名称候选：保留可查，不膨胀首页有效关系数。
- 代币创建事件离开扫描窗口，后来发生交易：统计和详情继续更新。
- 重放事件/服务重启/网络失败/短链重组：不重复计数、不跳过区间，状态可解释。
- 测试生成超过 400 条候选及 2,000 个资产：旧详情仍可查，分页只是展示限制。
- 毕业之后仍有成交，已验证关系失效之后保留历史与失效事件。
- API 失败不变成 0；不完整采样不宣称全天或全历史。
- 股票休市、缺样本、供应变更、篮子调仓不会产生误导性指数或相关数字。
- 任何跨链统计都保留各来源的独立明细和覆盖范围；只有口径、时间和资产身份一致时才聚合，且只有完成数据/证据/解释闭环的来源标注为已接入完成。

本轮只形成设计基准，不修改运行代码、不部署。下一轮实现按这些验收条件核对，而不以“页面有数字”作为完成标准。
