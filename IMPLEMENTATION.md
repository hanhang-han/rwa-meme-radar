# 2026-09-15：多源行情、持有人与股票参考数据接入

- 新增 CoinGecko Onchain/GeckoTerminal 与 DexScreener 链上行情适配器，按链和合约地址批量轮换候选资产与已核验池。X Layer、BNB、Robinhood Chain 使用各自网络 ID；字段按来源与观察时间择优，跨来源不相加。
- 候选资产可补价格、市值、24h 成交额、买卖次数、总次数、持有人、Top 10 集中度和流动性；已核验池按精确池地址补流动性及池级成交。页面字段提示和关系证据显示实际提供方。
- 新增 Robinhood Chain Blockscout 通用 API 适配器，按合约补持有人数；由于服务器访问公开实例会受到 Cloudflare 拦截，生产环境需要 `BLOCKSCOUT_API_KEY`。密钥配置前，使用 CoinGecko 公共代币资料轮换补持有人数与 Top 10 集中度。
- 新增 EODHD 搜索、证券身份解析和批量延迟行情适配器。请求按股票身份去重，不按跨链部署重复计费；真实行情时间与本站观察时间分别保存。生产环境需要 `EODHD_API_TOKEN`。
- 新增独立持久化快照 `data/market-enrichment.json`。上游失败保留历史记录，0 与缺失继续分开，未配置来源明确显示未配置。
- CoinGecko Pro Key 可选；缺少 `COINGECKO_API_KEY` 时使用 GeckoTerminal 公共接口，三条链合计每轮只追加一条代币资料请求，避免共享 IP 的 429 限流。所有新来源独立于现有 OKX 每轮/每日限额。

# 2026-09-15：页面视觉层级与说明入口优化

- 审查发现：指标/表头/数据行重复问号，移动端问号放大到30px；Meme单行各生成一张表，列宽不一致；首页文案与搜索处于同一flex行；股票页重复标题；配对页多重卡片争夺空间。
- 每页仅一个「页面说明」，14px圆形符号配12px文字，保留32px点击高度。按页面汇集分区与指标定义；表头、指标卡、数据行不再重复说明入口。轮询不重建正在阅读的说明内容。
- Meme统一表头与列宽，数值右对齐；同名合约原位展开，保持独立CA和筛选/排序语义。网络及最低流动性收进更多筛选。
- 首页介绍/搜索分区；统一20px分区间距与标题字号；数据质量、来源和指标进度以轻量折叠项呈现。股票页保留单一标题，摘要列对齐；配对页压缩侧栏并调整窄屏堆叠。
- 类型检查、CSS语法与38项测试通过；线上验收另见本次交付说明。浏览器工具曾连接超时，不将DOM测试等同于视觉验收。

# 2026-09-15：栏目说明与数据可用性核查

- 所有主要栏目与关键指标增加统一“？”说明，解释用途、口径、操作与缺项。弹窗支持中英文、Esc、焦点回到触发按钮，不产生上游请求。
- 核实上游数字代码 1 / 1024 / 1038 对应长江和记实业 / 快手 / 长江基建集团，显示 HKEX 市场和补齐代码，支持公司名称搜索。映射同时检查 tokenSymbol；其他未核实数字代码明确标注名称待核实，不凭数字推断市场或发行授权。
- 新增可展开的数据可用性面板，以 15 分钟窗口分别统计有效新鲜值、历史值、缺失值；0 与缺失分开。股票行情保留独立字段时间，目录数不当成可用关系资产数。
- 修复复核排队：只读 RPC 配对复核提前执行，每链每轮最多16条；付费采集分配成交8、池维护6、发现4的阶段上限，嵌套限额同时扣减网络/全局预算，未提高每轮70/每日8000的现有上限。
- 元数据按持有人字段时间独立刷新；买卖次数仅热门榜接口提供，调整为每轮预算内刷新该榜，非榜内资产仍可能缺失；不把搜索接口或部分成交分页误当成24小时买卖汇总。部分完成记录真实进展时间。仍不承诺全目录5分钟刷新，完整覆盖需要按实际额度和服务优先级继续规划。
- HTTPS 验收发现 cliperx.com 证书过期，已使用服务器现有 ACME 配置续期至 2026-12-14，并修复主域名配置未被已有 ubuntu 续期任务覆盖的问题。未更改业务路由。
- 成交展示优先呈现批量行情中的24小时总次数，买卖拆分保留独立缺失状态，避免只留下两个空数值。
- scripts/audit-data.mjs 只读本站缓存接口，核对重复身份、关系引用、异常数值/时间，并抽查三链详情。现阶段定位为关系研究预览，不认定为全量实时行情或资金流产品。

# 2026-09-14：V2 优化版本

本节优先于历史实施记录。实施依据为 OPTIMIZATION_PLAN_V2.md；本轮上线核心数据与页面闭环，P2 项目不标记为完成。

- 已新增总览、股票聚合、独立配对分析、完整事件历史入口；旧 Meme 详情地址保留，主导航四个入口。来源能力按目录/行情/成交/关系分项展示。
- X Layer、BNB、Robinhood 共用参数化的 OKX 采集和链上地址核验流程；三条网络的 RWA 接口及 RPC chainId 已实测。记录按 chain + CA 隔离，同一部署多源观察去重；场内与 DEX 成交不混加。
- SQLite schema v2 原子迁移 facts、samples、trades、events 的身份键。部署前 VACUUM INTO 一致性备份；回滚同时恢复旧代码和兼容数据库。关系历史用时间+ID游标分页。
- 每轮完成后等待 300 秒，轮换网络顺序，维护篮子/已验证关系的行情优先；目录改为小时级复核，行情独立批量采样。每轮本地保护上限默认70请求，每日默认8000请求（UTC，重试计数），这不是已核实的账户官方限额，可通过 .env 调低或按实际额度配置。正常预算耗尽显示下轮续采，字段时间不伪装更新。
- 行情逐字段保存时间；沿用旧字段时不更新其观察时间。芯片篮子保留原成分、基期和历史，恢复优先采样；暂无合格成分的行业显示收集状态。
- 列表支持搜索、关系状态、网络、最低流动性与六种排序维度，URL保留筛选/分页。语言切换读取本地数据与详情缓存；tooltip、图表、空态和错误说明双语。头像有失败占位。
- 配对页展示核验地址、池结构/费率、已覆盖池份额、Top10集中度、共同采样价格趋势和有条件的跨市场溢价。Robinhood发行方推导价不与自己的参考输入制造零价差；仅同一部署的独立参考观察可参与计算。
- 新版通过 /api/dashboard 读取精简数据；响应压缩、10秒序列化缓存、图表异步加载。浏览器刷新与详情访问不触发付费上游采集。
- 已在服务器验证三链 OKX 资产页面HTTP200、页面标题及合约地址匹配；按钮使用明确的外部资产页面，不执行交易。移动端 App Universal Link 唤端仍未验收。
- 类型检查及32项单元/DOM集成测试通过；域名子路径的总览→详情→Meme→股票→配对冒烟通过，无脚本错误。浏览器可视化工具连接超时，未把DOM测试宣称为完整的真浏览器/移动端视觉验收。

仍有明确边界：仅主要池覆盖；第三方未提供的数据保持缺失；未自动识别的金库/回购/兑换关系、完整热度、Stock Flow、完整分母捕获率、独立池隐含价差、AI引用式叙事、Trigger图与高级持仓画像仍需后续数据能力。图表仅展示实际已有共同样本；不补造7天历史或不存在的股价。Stock Radar 标的映射仍主要依赖来源 stockCode，需要继续补充交易所/证券身份字段来处理歧义。

# 需求对齐进度

## 2026-09-10：X Layer 关系发现与证据页面已实现

此节优先于下面的旧版实现记录。

- 真实 OKX RWA 已联调：需要 category=47；统一签名请求队列及限流退避。缓存目录保存至 data/okx.json。
- 服务端已输出来源中立的 `unified` 数据契约：资产、股票部署、关系和事件均保留内部 provider、chainId、chainName、CA 与时间。页面只按资产和关系展示，不把链或来源作为筛选项和列表列。
- Robinhood Stock Tokens 已接入公开目录和全量报价：合约按股票代码与既有目录同组展示，报价按官方 `currentMultiplier` 换算。`dailyTradingVolume` 是标的股票市场成交量，不能冒充链上代币成交额，因此 `volume24h` 保持为空。Robinhood 的关联 Meme 池扫描尚未接入；公共 RPC 受限，后续需补独立索引或 DEX 适配器。
- Binance bStocks 已接入公开 Spot 报价和已核验的 BNB Smart Chain 合约目录。Spot 的 `quoteVolume`、成交笔数显式标为“场内”统计，不能混同为 DEX 池成交或关系证据。统一页面顶部展示已接入的 OKX、Robinhood、Binance 目录及各自更新状态。
- 从股票代币反查 OKX 前 5 流动性池，在 X Layer RPC 同一区块读取 token0/token1，支持 asset()/underlying() 包装股票映射。链名或符号不能代替地址验证。
- 热门榜为补充发现，行情/成交/风险/公司资料分别接入。首页是关系事件，Meme 页支持重点关系、全部已核验、名称候选、全部发现、历史，同名组保留独立 CA；股票按代码聚合；详情显示真实证据、行情、成交、风险标签和公司资料。
- 重点关系的初始展示门槛为配对池流动性 $1,000；低流动性配对仍保存并可查看。基础报价资产（包含 xBTC）从 Meme 关系汇总排除。
- data/research.sqlite 保存资产、关系、扫描时间、去重成交、信号和 5 分钟价格样本。列表分页不删除历史，成交分页缺口保存续取游标。统计区分 OKX 24h 聚合与本项目采集记录，后者不宣称全历史完整。
- 股票轮换扫描每批 4 个；关系资产优先更新，同时轮换服务较早等待的候选。执行完一轮后约 5 分钟开始下一轮，实际数据时效逐项显示；不是全目录 5 分钟刷新。
- 为控制 OKX 调用配额，OKX RWA 目录与 X Layer 关系采集均在启动时执行一次，之后每轮完成后等待 5 分钟；单轮仍会批量请求必要的行情、成交和核验接口。
- 篮子首版使用 3 个合格成分门槛、固定基期 100、OKX 市值权重，成分缺失时暂停。没有达到门槛的行业显示成分进度；不伪造指数。
- 篮子成员资格使用已持久化且有效的关系/行情记录；轮换采集导致行情暂时变旧时保留成员，但指数值暂停并显示待更新原因。
- 仍未完成：独立股票现货行情/相关性、股票侧完整流动性分母/捕获率、LP 资金流、完整热度输入、金库/回购/兑换自动发现、AI 生成服务。当前解读明确标为规则说明。
- 当前池覆盖仅 OKX 每资产前 5 池，V4 无独立池地址等接口会标为部分覆盖；未查询到不等于全链不存在。
- 运行时要求 Node >= 22.13；ecosystem.config.cjs 固定服务器已安装 Node 22，避免 PM2 继承 PATH 后用 Node 20。npm run deploy 保留 .env 和数据文件。
- 验证包含类型检查、真实接口和持久化/渲染测试。测试只使用假凭据，NODE_ENV=test 禁止写 OKX 生产快照。

## 2026-09-09 旧版记录（以下限制部分已由上述实现解决）

当前实现四个 hash 路由：#live、#meme、#stock、#detail/56/<CA>。
首次打开默认选择可用数据源；OKX 尚未接入时展示 BNB Chain 参照，并明确标注。手动选择的网络会保存。不能把当前 BSC 数据当成 X Layer 数据。

## 启动和验证

Node >= 20.12；`npm start`，默认 3456 端口。支持 PORT 环境变量。
复制 `.env.example` 为 `.env` 并在本地填入 OKX 凭据。服务端加载 .env，凭据不发送到浏览器。
`npm run check`、`npm test`。

## 数据接入

OKX RWA：GET https://web3.okx.com/api/v6/dex/market/rwa/tokens，chainIndex=196，limit=100，游标分页。
使用 HMAC-SHA256 / Base64 签名，包含完整路径和查询参数。响应按 chainIndex 二次过滤。
状态区分未配置、成功、失败、保留旧数据。空字符串价格为 null，不能作为零。
官方接口文档：https://web3.okx.com/zh-hant/onchainos/dev-docs/market/market-rwa-token
已用模拟响应测试分页、过滤及错误保留；尚无凭据进行真实 OKX 联调。

## 关系体系 v1（产品草案）

名称/叙事、直接配对、金库持仓、回购联系、兑换/权益、统计联动。
原需求未逐项定义六类，此列表为实现草案。
名称匹配只生成候选，不代表资金联系、官方认可或安全性。
直接配对验证当前覆盖 BSC PancakeSwap V2，同股票代码最多 4 个股票代币。
读取 Factory getPair，再核对 token0、token1。查询失败与限定范围未发现分别显示。
找到池仅证明地址配对，不证明流动性充足或合约安全；不覆盖 V3、其他 DEX、金库与回购。

## 指标口径

- Pearson：滚动 4h、5 分钟桶，至少 12 个共同样本。使用价格水平，不是 beta；恒定价格返回空值。Meme 价格取独立 USDT/USDC 池，避免从股票报价反推引入机械相关。稳定币按 $1 估值，股票锚定价采样时不得超过 5 分钟。
- 行业篮子：只纳入已验证的候选；芯片、交易所、支付、托管、加密国库。全体成分必须有共同样本。以保留窗口内最早共同时间为基期 100，按基期 totalSupply × price 加权（总供应市值代理，不是流通市值）。成分或保留窗口变化可改变基期，目前为方法打样，不应跨重构直接比较。
- 热度：计算函数 v1 为 40% 成交量增速 + 30% 持有人增速 + 30% 股票锁仓。各项分别以 +100%、+20%、$1m 归一化并截断到 [0,1]。缺少任意输入返回空值；完整数据采集未接入，因此页面不输出数值。
- 捕获率：配对池占用股票流动性 / 同口径股票总流动性；分母缺失或不一致返回空值。尚无完整池覆盖，因此页面不输出数值。
- Stock Flow：未接入已验证股票配对池的完整资金流事件；当前空值，不能用锁仓快照冒充资金流。

## 仍待完成

X Layer Meme 发现源、X Layer 配对验证；真实 OKX 联调；AI 服务及基于来源的股票背景卡；金库、回购、兑换证据；欺诈检测；完整持有人与成交量采集；Solana / Robinhood 参照。
目前关联解读为明确标注的规则说明，非 AI 生成。
详情依赖当前新币窗口，窗口之外显示不可用；长期资产索引仍待建设。
新增页面目前以中文为主，旧 BSC 组件保留原有中英文切换。
部署脚本在服务器保留回滚包，并在重启前保存真实 API 快照；快照恢复保留原始采样时间，每分钟原子写入 data/state.json。
