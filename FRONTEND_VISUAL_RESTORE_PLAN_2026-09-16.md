# Vue 重构后前端视觉与交互还原方案

## 任务目标与边界

保持当前 Vue 3、Pinia、Vue Router、FastAPI 和实时更新架构，恢复重构前最后一版网站的页面结构、视觉样式、内容层级和交互。不是重新设计，不采用此前实时行情调研中尚未落地的新布局，也不退回旧版整页 innerHTML 渲染。

本文件是交给修复 agent 的实施方案。本次仅检查源码和线上页面，没有修改业务代码或部署。

## 可信基线

- 检查时 HEAD：`2caafa6`，工作区原本干净。
- Vue 重构前最后提交：`2ac7a7e`（紧接着是 `49f62b7 P0 skeleton`）。`git diff 2ac7a7e HEAD -- public` 无差异，当前 `public/` 可作为旧版参考。
- `public/style.css` 与实际导入的 `web/src/styles/main.css` **SHA-256 完全一致**：`546dae47c9b374e600f956f4c2f568dab809c5d63461c82a5010911499296a9d`。
- `web/src/main.js` 导入的是 `./styles/main.css`，未导入 `web/src/style.css`。后者虽然残留 Vite 默认主题，但不能把它当成当前视觉回归的根因。
- 旧版最终呈现由 `public/index.html`、`public/workspace.js` 和 `public/radar-v2.js` 共同决定。尤其应以 `radar-v2.js` 的最终覆盖结果为准，不能照抄 `workspace.js` 早期占位模板或恢复已隐藏的旧模块。
- 浏览器已核对线上首页、Meme 全部资产、交易池分析入口。其余页面差异来自源码比对，实施时需要逐页截图验证，不能把本报告当成已完成全站像素验收。

## 修复顺序

P0：公共外壳、首页结构、Meme 单表、真实配对入口及链接格式。

P1：股票页、详情页、配对分析缺失模块、历史事件、统一字段呈现。

P2：图表交互、帮助弹窗、中英文和响应式逐页对照验收。

### 1. 公共外壳与 CSS 适配

修改：`web/src/App.vue`、`web/index.html`、必要时 `web/src/styles/main.css`。

对照：`public/index.html`、`public/workspace.js`、`public/radar-v2.js` 的 `render()`。

- 品牌标题由 `RWA Meme Radar` 恢复为 `STOCKSMEME RADAR`，浏览器标题与旧版保持一致。
- 恢复旧版实际可见的页面同步时间、刷新按钮及语言切换反馈。同步时间只表示页面同步，不伪装行情时间。不要恢复旧版最终已隐藏的 BNB 报价和健康条。
- 四个主导航与旧版一致：主页、MEME、股票、交易池分析（具体中文按旧版运行结果）。发现记录保持旧版的次级入口；新版新增顶层“事件”不是无差异迁移。
- 为路由内容恢复 `.view` 容器，确保 `.view .panel`、`.view .kpi-value`、`.view .x-empty` 等规则继续命中。可在 RouterView 外包装，或者每页根节点统一处理。
- 恢复公共页脚“关系有证据，数字有来源。历史持续保存，缺失不等于零。”及对应英文；页脚以 `radar-v2.js` 最终覆盖文字为准。
- 删除 App 中与全局导航重复、数值不同的 scoped 覆盖，按旧版最终 computed style 决定间距，不再堆补丁。
- 旧版主要设计数值：背景 `#0a0e14`、面板 `#10161f`、边框 `#1d2736`、正文 `#c9d4e3`、次要文字 `#91a0b5`、强调 `#b9fa6a`；body 14px/1.5、20px 外边距；面板 20px 内边距、20px 节间距、10px 圆角；KPI 数值 26px。

### 2. 首页：恢复模块顺序和完整结构

修改：`web/src/views/LiveView.vue`。可提取 `EventRow.vue`、`SectorBasket.vue`、`AssetLogo.vue` 等组件。

对照：`public/radar-v2.js:132` 附近的 `overview()`。

旧版顺序必须恢复为：

1. Hero 标题、页面说明、右侧搜索。
2. 今日变化摘要（保留旧版长内容折叠行为）。
3. 四张 KPI。
4. 双列：左侧最新发现与变化，右侧关联 Meme 成交榜。
5. 独立整行“行业主题指数”。
6. 各链数据概况。
7. 页底数据来源说明。

当前 Vue 把行业篮子塞进右侧、摘要移到 KPI 后、成交榜移到底部，并漏掉链概况和来源说明。属于结构回归，不是 CSS 颜色问题。

具体要求：

- 最新发现恢复 `.x-signal.v2-event` 与 `SYMBOL ↔ TICKER` 的紧凑行、状态标签及“全部历史”入口；由旧版 6 行控制首屏密度，不照搬早期 `.x-event-dot` 大行。
- 成交榜恢复 logo、币名、关联股票、成交额、链与字段时间。
- 行业指数恢复 `.v2-baskets > details.x-basket > summary`：当前/历史指数、成分门槛、展开后的走势图与成分权重。当前 `.x-basket-row` 在基线 CSS 中没有对应样式，导致“芯片50.65”挤在一起。
- 保留 `lastValue`、历史标识与 `history/components`；筹备中的主题不能用 `members/3` 替代指数值混排。
- 如果 FastAPI 未提供其中某个字段，记录为接口兼容缺口；不得用删除模块或填 0 的办法通过还原验收。

### 3. Meme 列表：一张表、一个表头、一个滚动容器

修改：`web/src/views/MemeView.vue`、`web/src/components/MemeRow.vue`。

对照：`public/radar-v2.js` 的 `memePage()`；`public/style.css:270` 附近的单表规则。

线上已确认当前逐组生成 table，重复表头，同名组折叠后行情整行消失。

恢复结构：

```html
<div class="scroll v2-table-scroll">
  <table class="tbl v2-meme-table">
    <thead><!-- 全表唯一表头 --></thead>
    <tbody>
      <!-- 每组始终显示第一条资产的全部行情 -->
      <!-- 第一列放“+ N 个同名合约”按钮 -->
      <!-- 展开时追加 .v2-member-row 子行，不产生新 table -->
    </tbody>
  </table>
</div>
```

- 恢复 8 列顺序、数字列右对齐、sticky 表头、1050px 最小表宽、72vh 表内滚动。
- 恢复页面标题和说明；恢复“更多筛选”中的网络、最低流动性，以及“首次收录”排序。
- 恢复 28px logo/占位图，关系链接逐行展示，状态使用旧版文字而非裸 `✓/?`。
- 恢复每字段来源/时间 tooltip、历史值标记、首次收录时间，保持缺失值与真实 0 的区别。
- 行、关系匹配和闪烁缓存键都用链 + 合约，避免同地址跨链串行或误闪。
- 验收：20 组资产只出现一个表头；折叠和展开都保持 8 列完全对齐；同名组折叠时仍可看到代表资产行情。

### 4. 股票页：恢复摘要列与内容密度

修改：`web/src/views/StockView.vue`。

对照：`public/radar-v2.js` 的 `stockPage()`。

- 恢复外层页面标题、说明和 `.v2-stock-toolbar`，不要用普通 `.x-toolbar` 顶替。
- 旧版 summary 是股票身份、代币版本数、关联 Meme 数，外加 CSS 的 +/-。新版额外增加 24h 金额，超过旧版四列网格；删除该擅自增加项，按基线恢复。
- 恢复 20 组分页，避免一页堆出全部股票。
- 恢复股票名/市场代码的两层文字，中文名由已核实身份优先；不同市场不因代码相同合并。
- 代币版本表恢复源代码、价格时间与价格性质（DEX/场内/发行方参考）、参考价来源和时间、溢价缺失原因、成交额市场及时间、复制地址与目录记录状态。
- 不将不同市场的成交额相加后作为新的汇总展示。

### 5. 详情页：恢复图表旁栏和模块顺序

修改：`web/src/views/DetailView.vue`、`web/src/components/CandleChart.vue`。

对照：`public/radar-v2.js` 的 `detailPage()`。

- 标题恢复 logo、名称、链标签和页面说明；当前“持续追踪”标签不是基线的链标签。
- 恢复“在 OKX 查看资产”、部分采集失败/保留历史的状态信息。
- 旧版顺序：身份与地址 → 关系证据 → 数据解读 → KPI → 价格图/集中度双列 → 最近成交 → 发现与核验记录。
- 四个 KPI 保持最新价、成交额、成交次数、持币地址数；当前第四项改成“流动性”改变了内容。
- 价格图放回 `.x-grid` 的左面板，右面板恢复“前10大地址持仓占比”、进度条、采集时间和缺失状态。普通“风险等级”卡片不能代替。
- 图表模式/周期按钮放回 `.panel-head` 右侧，380px 图高按基线保留；Vue 组件可使用 slots 实现，不破坏图表增量更新。
- 最近成交按旧版列顺序与宽度恢复；新增成交价列属于设计变更，严格还原阶段先按基线处理。
- 时间线复用统一事件行，避免退化为普通段落。

### 6. 交易池分析：修复入口，并恢复被省略的分析区

修改：`web/src/App.vue`、`web/src/router/index.js`、`web/src/views/PairView.vue`、`web/src/utils/format.js`。

对照：`public/radar-v2.js` 的 `pairPage()` 及 `render()` 的无参数 pair 分支。

- 删除导航与 KPI 中固定 `/pair/196/0x0`。线上已确认它落在空状态。增加有效的 `/pair` 总入口，恢复旧版“选择资产或配对”：搜索框和最多 30 条已核验关系链接。不能以假地址补必填参数。
- 保留老链接 `#pair`、`#detail/...` 的兼容处理；切换链、股票、池时都更新选中状态和数据，不能只 watch stock。
- 恢复独立页标题和 `.v2-pair-layout`，左侧 `.panel` 配对列表宽 220px、sticky；右侧为多个独立面板，不能把整个分析页压进一个 panel。
- 右侧恢复：选中关系证据、核心事实（池流动性/池成交额/池成交次数）、股票价与代币价对比、流动性池分布、Meme 与股票代币涨跌对比、前十大地址集中度与交易活动。
- 当前 Vue 只读 stockDetail 并循环关系证据，缺少选中 Meme 详情及其 pools/samples/risk。这是功能迁移遗漏，需要补齐数据绑定后才能还原视觉。
- 复原指数/对比折线时可使用 ECharts Vue 封装或适合的已有图表封装；当前 Vue 已移除 ECharts 引用，必须显式补回所需依赖和生命周期，不可直接调用未加载的 `window.echarts`。

### 7. 事件历史：恢复紧凑样式及跨链覆盖

修改：`web/src/views/EventsView.vue`、必要时 `web/src/api/client.js`。

- 使用旧版 `eventHtml()` 的紧凑双资产标题、事件标签、链与发现时间，恢复对应跳转语义。
- 默认跨三条链读取，分别保留游标，按链 + 事件 ID 去重后按时间排序。
- 当前 `route.query.chain ?? '196'` 默认只请求 X Layer，造成“历史少了”的观感，与样式问题必须区分。
- 保留刷新/加载更多、错误与真正空状态；不能把已加载无记录长期显示为“正在读取”。

### 8. 共用组件、路由与双语

修改：`HelpTip.vue`、`KpiCard.vue`、`RegistryBadge.vue`、`utils/format.js`、`i18n/index.js` 及调用处。

- 建议新增 `AssetLogo.vue`、`FieldValue.vue`、`EventRow.vue`、`PageHeading.vue`、`PageGuide.vue`，集中迁移旧版 `logo/field/eventHtml/help`，不重复手写不同版本。
- 保留旧版轻量模块问号：20px 点击区、11px 字形；页面级说明采用 `.v2-help`，不要所有地方都使用一个裸问号。
- 帮助弹窗恢复分主题内容、关闭后的焦点归还、Esc 关闭、背景滚动锁定和中英文同步。当前 Esc 只监听触发按钮，不足以覆盖弹窗内部焦点。
- 路由格式统一：传给 `RouterLink :to` / `router.push` 的应该是 `/detail/...` 或路由对象；普通 `<a href>` 才使用 `#/detail/...`。当前 `detailLink/pairLink` 返回 hash 链接，却被大量传给 RouterLink，应统一修正，并保留普通链接兼容。
- `date()` 不再读取未同步的 `window.__LANG`，使用当前响应式语言状态。
- AI 简报、解读在切换语言时重新获取或读取对应语言缓存；当前首页仅 onMounted 获取，正文不会随按钮切换。

## 验收方法

1. 用 `2ac7a7e` 的旧 public 页面作为只读参考，不切换/回滚当前工作目录。用独立临时目录或对照工具启动旧 UI，接冻结的同一份 API 样本，避免数据与时间变化干扰比较；不要额外启动旧采集器消耗额度。
2. 旧、新两套使用同一浏览器、视口、语言、滚动位置、筛选和展开状态。建议桌面 1440×900、1280×800，移动 390×844，并覆盖 1100/800/480 断点附近。
3. 必须逐页截图：首页、Meme 单合约/多合约展开、股票及代币版本展开、有数据的配对分析、详情 K线/分时、事件历史、帮助弹窗。
4. 区域级对比模块顺序、x/y/宽高、字号行高、色值、边框、间距、图高和表头。字体抗锯齿可容忍少量像素差，缺模块/错列/换布局不能容忍。
5. 交互回归：搜索、过滤、分页、深链接、返回列表、复制、语言切换；价格推送时不重建整页、不丢展开状态和图表缩放。
6. 测试桩可用于冻结对照样本，但生产页面不得补造数据。接口缺字段单独列兼容问题，不通过隐藏内容掩盖。
7. 编译成功不是验收完成。最终交付应列出改动文件、逐页前后截图、交互检查结果和未解决接口差异。

## 可直接发给修复 agent 的指令

请按本文件修复 Vue 重构造成的视觉和交互回归。保留当前 Vue/Pinia/FastAPI 与实时更新架构，以提交 2ac7a7e 的最终运行页面为视觉基线；当前 public 目录与该基线无差异，可只读参考。main.css 与旧 style.css 完全相同，优先修复模板结构、缺失模块、class/容器契约和路由，不重新设计主题。先完成公共外壳、首页、Meme 单表和配对入口，再逐页迁移遗漏的展示与交互。不要恢复旧版被最终渲染逻辑隐藏的模块，不要把更早期 workspace/xlayer 模板当成最终基线。逐页完成同数据、同视口的截图对照及中英文/移动端验收后，再给出具体修复结果。
