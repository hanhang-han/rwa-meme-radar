# STOCKSMEME RADAR 网站优化规划 V2

日期：2026-09-14。状态：核心功能已于当日实施上线；后续栏目说明及数据核查详见 IMPLEMENTATION.md。未完成项以实施记录为准。

依据：三份 V2 文档、用户此前明确的产品要求、当前项目源码，以及 2026-09-14 18:35（北京时间）附近获取的线上 `/dashboard/api/state` 快照。线上数字仅为本次观察，不代表长期规模或全链覆盖。

## 1. 目标与规划原则

网站应帮助用户完成一条清晰路径：发现股票相关 Meme → 判断关系是否有真实证据 → 比较资金、交易与流动性结构 → 查看具体配对 → 打开正确的外部资产页面。

第一优先级是让已有数据形成可信、持续更新的产品闭环。数据规模应以去重后的资产和有效关系衡量，不能以增加来源标签或混合不同市场成交量来扩大。

保留用户已确定的约束：默认聚合所有来源，不设主场链；数据同构；来源可追溯；中英文即时切换；额度受控的采集以 5 分钟为基本节奏；历史线索和篮子持续保存。

三份文档的使用方式：需求迭代清单作为需求追踪表；STOCKSMEME PRD v2.0 作为详细功能参考；RWA 迭代 PRD 作为演示范围与排期参考。出现冲突时，以用户已明确要求、可核查的数据口径和本规划中的统一规则为准。文档引用的旧版 PRD 未在本轮完整核对，不能把当前六类关系草案等同于已核定的原始定义。

## 2. 当前基线：保留什么，补什么

| 项目 | 本次核查结果 | 规划动作 |
|---|---|---|
| 数据真实性 | 源码已有真实 API 适配器，线上返回三来源记录；不能沿用“全站静态、从零接入”的判断 | 复用并审计现有管道；本轮快照核查不代替逐字段上游对账 |
| 股票目录 | 1,105 条来源记录：OKX 836、Robinhood 194、Binance 75；这些不是 1,105 只去重股票 | 分清股票标的、链上部署、场内交易品种和来源观察 |
| 统一关系覆盖 | 统一数据中的 62 条关系记录均来自 OKX/X Layer，其中当前摘要计入 56 个已核验池 | 把统一关系管道扩展到其他可验证网络；不能把目录接入标成完整关系接入 |
| 指标覆盖 | 184 个候选中，43 个缺买卖次数、76 个缺持有人、3 个缺成交额 | 按字段显示状态，定位接口不支持、排队、失败和覆盖缺口 |
| 流动性覆盖 | 56 个当前已核验池中只有 12 个有计入摘要的新鲜估值 | 优先维护有效关系池，给摘要显示估值覆盖率 |
| 行业篮子 | 芯片有固定 3 成分，等待 2 个行情更新；另四类尚无合格成分 | 优先刷新固定成分；保持历史与成分，不用减少成分来制造可计算结果 |
| 持久化 | `research-store.ts` 已有 SQLite 事实、事件、成交、采样表 | 扩展多链唯一键、游标、字段时间和迁移保障；不另起一套割裂历史 |
| 更新机制 | OKX/X Layer 每轮完成后等待 5 分钟；股票每批轮换 4 个，并非全目录每 5 分钟更新 | 统一预算和任务优先级，显示字段时间与完整扫描周期 |
| 页面 | 已有关系流、同名折叠、股票聚合、证据详情和双语；Stock Flow 与完整热度仍缺输入 | 复用有效内容，补配对分析和可信指标，减少大片等待卡 |

源码核查的关键风险：`xlayer.ts` 仍固定使用链 196，资产存储多处仅以地址为键；`unified-state.ts` 主要合并股票目录，Meme 与关系仍继承 X Layer；Stock Radar 汇总部分 `volume24h` 时未按场内/链上口径拆分；部分行情字段继承旧值，但只使用资产级更新时间，可能掩盖字段本身过期。

## 3. 统一文档冲突

| 冲突点 | 本轮采用的规则 |
|---|---|
| 15 秒/60 秒刷新与用户的 5 分钟限制 | 不提高付费采集频率。前端可读取本地缓存并更新状态，但不因刷新页面或切语言触发上游采集 |
| X Layer 默认与全链平权 | 默认全部资产聚合；链属于资产身份信息。高级筛选可折叠，不恢复以链或提供方分割的主导航 |
| 所有字段都来自 OKX 与已有多源接入 | 保留来源中立适配层。验证 OKX 对其他链的具体接口和真实资产覆盖后再扩展；不假定换 chainId 即全部可用 |
| 三层价差与双价差 | 三类资产是股票标的、股票代币、Meme。它们不是三个可以直接相减的同单位价格；统一使用下文两个有条件的价差定义 |
| 股票代码、CA、配对的跳转相互冲突 | 股票名称/代码进股票视图；股票代币 CA 进该部署的配对分析列表；具体池进配对分析选中态；复制 CA 使用独立按钮 |
| 默认热度排序但完整热度缺输入 | 当前默认按证据等级，再按同口径 24h 成交额；明确标注排序名称。完整热度可复算后再开放，缺值排末尾 |
| 持仓分布与 LP 结构混称“筹码” | 分成两个模块：持仓地址集中度、流动性池结构，各有不同分母和解释 |
| 五个业务行业篮子与接口九类行业 | 业务篮子保留五类；上游行业为资产分类元数据，通过版本化映射关联，不能直接替换业务篮子 |
| 断网清空与保留上次数据 | 保留最后成功值及其时间，标记过期/失败；从新鲜统计中排除，不用 0 或无时间旧值兜底 |
| Arc 上线与“自动接入就绪” | 只展示经核实的准备状态。未经接口与资产验证，不承诺自动收录，不照搬文档中的上线日期或就绪声明 |
| 观察 10 分钟必须有变化 | 验收刷新任务、成功时间和事件证据；市场无变化时不能为了满足验收生成事件或修改数字 |

## 4. 页面规划

### 4.1 导航与统一交互

建议四个一级入口：**总览｜Meme 雷达｜股票雷达｜配对分析**。

Live Radar 作为总览里的独立“最新线索”区，提供查看全部的事件页；Meme Detail 作为上下文详情页。保留旧 `#live/#meme/#stock/#detail/...` 地址兼容，新增明确股票和配对地址；所有路由、资源与接口兼容 `/dashboard/` 子路径。

顶部固定：品牌、股票/名称/CA 搜索、中/EN、简洁的数据更新时间入口。数据来源面板展示提供方及能力状态；每条记录用紧凑链标识确认身份，来源和字段时间可展开查看。主表不额外增加宽大的来源列。

搜索按股票标的、股票代币、Meme 分组。地址存在跨链重名时列出真实匹配项，不默认当成 X Layer。语言切换使用已加载数据即时重绘，保留路由、筛选、分页、展开项和选中配对；原始币名和 ticker 保留，已有可靠中文公司名优先显示。

### 4.2 总览：现在有什么值得看

- 首屏：有效关联 Meme 数、已核验关系池数、已覆盖池流动性、今日新增关系。每项点击可到对应明细，统计对象和覆盖率一致。
- 主区：最新关系事件流 + 值得查看的关系排行。事件区分新配对、首次核验、流动性变化、证据失效，并提供时间和证据入口。
- 次区：行业篮子趋势与成分、跨链覆盖概览。行业未满足要求显示成分进度，成熟篮子优先展示。
- 来源状态收进可展开面板：目录、行情、成交、关系核验各自显示已接入/部分覆盖/未支持，不能只给一个绿色“已更新”。
- 尚不可计算的完整热度和 Stock Flow 放入紧凑“指标建设进度”，不占首屏大 KPI。

跨链图只聚合一致口径的数据。链上与场内成交分别展示；同一池由多个提供方返回时只计一次；同一池不能因同时出现在两个资产的池列表中重复累计。

### 4.3 Meme 雷达：比较与筛选关系资产

默认展示有证据的关系，其他视图为待核验、名称线索、全部发现、历史。同名只折叠展示，每个链+CA 独立保存和统计；没有股票关键词但实际配对的资产也必须被发现。

主表建议列：头像/名称/短 CA、关联股票、关系类型与证据状态、配对池流动性、24h 成交额、24h 买/卖、最后更新。市值、持有人、首次发现时间和 5m/1h 成交额放扩展列。

筛选：关系状态、关系类型、股票/行业、流动性范围；高级筛选再提供链、发行方、市值范围。排序和筛选写入 URL，先过滤和排序全量结果，再分页；不能只排序当前页。零成交、缺失成交、不完整成交分别呈现。

### 4.4 股票雷达：一只股票的完整关系图

第一层以股票标的为单位，显示中文公司名/ticker、资产类型、关联 Meme 数、有效池规模、同口径关联成交和最近事件。标的身份不能仅依赖 ticker；交易所、发行资料与标的类型参与匹配，避免同代码和非上市权益误归类。

点击展开第二层：各发行方/网络的股票代币或场内品种，显示 CA、价格、每枚代表的股份数量、参考股价与行情时间。跨链代表采用同一表格样式，不强制按链拆成几块空栏。

第三层在各真实部署下显示关联 Meme；同时提供该股票全部关联资产视图。明确区分名称候选数和已核验数。未知覆盖写“尚未发现/未覆盖”，不能据此断言该链不存在对应资产。

### 4.5 配对分析：重点新增的独立页面

以股票代币部署进入时，左侧是关联池列表，右侧为选中池的分析；直接以池地址进入时定位同一视图。一级入口未选对象时提供搜索与已核验配对，不出现空白详情。

首屏回答：双方是什么资产、关系如何证明、流动性多大、数据多久前有效。展示双方 CA、DEX、池地址、核验区块/时间、资产组成和已覆盖范围。

核心内容依次为：

1. **流动性结构**：各已覆盖池规模、双方储备/占比、费率、池级成交额（仅有同池来源时展示）。最多只取得 Top N 池时写“已覆盖 N 池”，图表分母为已覆盖池。
2. **价差与走势**：符合条件的两个价差、同基准归一化走势、1h/24h/7d；缺少对应历史时禁用窗口并解释，不补画历史。
3. **持仓集中度与活动**：Top 10 占比、地址数趋势、成交活动。LP 地址、销毁地址等仅在可核验时标注，不将大地址直接等同于个人持有者。

同一股票代币的跨市场溢价是部署级指标，放在页面顶部；不能给每个 Meme 配对复制同一个数字却声称它是各配对独立价差。完整热度未就绪时显示实际成交/持有人趋势及缺项。费率不是收益率，资产总成交额也不能充当某个池的成交额；本轮不输出缺少依据的 LP APR。

### 4.6 Meme 详情：解释这条关系为何成立

信息顺序：关系结论 → 证据卡与规模 → 行情/成交 → 关联股票 → 时间线/风险与资料。

无成交但有证据时突出证据；只有名称线索时显示候选原因和验证状态；旧资产显示最后活动及历史；接口失败保留明确标记的历史值。缺图表时用简短原因和可用事实，不留一屏空坐标。

保留规则解读的正确名称。后续接入 AI 时，输出事实、解释和未证明部分，附证据版本和生成时间；不能把模板文字改名为 AI。

## 5. 数据与指标设计

### 5.1 同构数据，而非强行补齐数值

采用统一实体：股票标的、链上部署、场内品种、池、行情观察、成交、关系证据、事件、时间序列、篮子版本。链上部署键为 `chainId + normalizedAddress`；场内品种使用 `venue + instrumentId`，不能伪造链或 CA。关系和事件明确引用实体键。

同一个资产有多个来源时保留多份原始观察，根据字段的市场范围、时效和质量选取展示值；来源优先级固定且可审计，不简单取最大值、求和或平均。底层迁移保留旧地址 URL 到新实体的映射。

每个数字保存：值、单位、提供方、市场范围、原始观察时间、本次取得时间、统计窗口、覆盖率、状态、缺失原因。采用新鲜/部分覆盖/过期/未支持/失败/不适用等状态；只有完整统计范围内确认无事件才能显示 0。

### 5.2 采集与额度

- 保留 5 分钟基本节奏；统一管理 OKX 目录和关系任务，避免多个循环各自消耗预算。明确每轮可调用数、每分钟限制、每日额度、重试额度；实际账户上限待核实，不预设数字。
- 优先级：固定篮子成分与有效关系行情 → 已核验池复核/成交补缺 → 新配对候选 → 目录和休眠资产。为低优先级保留配额，避免永远饿死。
- 每个字段按需要设置 TTL；公司简介、图片和稳定元数据更低频。重点资产更新目标为一个或两个采集周期，必须先完成请求成本核算再承诺覆盖规模。
- 周期超过 5 分钟时不启动重叠任务；保存计划时间、开始/完成、耗时、下一轮、队列年龄。不能再让用户把“服务刚更新”理解为“所有记录刚更新”。
- 每 5 分钟一个理论周期约为每天 288 轮，日调用估算为各任务轮次×每轮请求量加分页与重试；受额度约束时降低覆盖批次并如实显示延迟。
- 页面刷新、切 Tab、切语言只访问本站缓存。新地址查询任务去重并加入预算队列。

### 5.3 持久化与最新线索

在现有 SQLite 上迁移多链键，采用事务与备份，迁移前后核对行数、唯一性和抽样旧详情。分页和缓存淘汰不能删除业务历史。

事件分清链上发生时间与系统发现时间。相同事实重复采集不生成新事件；状态改变生成新版本，临时接口失败不直接判关系失效。提供游标分页和断点续取；前端有新事件时显示“新增 N 条”，保留用户当前阅读位置。

成交按链、交易哈希、日志/上游事件标识去重，多笔 swap 与交易笔数采用明确口径；补采游标与数据事务共同提交，处理失败重放和可检测的链重组。持有人/价格历史从真实样本开始，不使用当前数据反填过去。

### 5.4 价差的可计算定义

跨市场溢价：令每枚股票代币代表 q 股，代币链上美元价为 T，股票参考价为 S，则 `100 × (T / (q × S) - 1)`。要求 q、币种、价格类型和时间可核查；停牌、休市、参考价陈旧分别标注。没有公开市场现货价的标的显示参考估值类别，不冒充实时股价。

池隐含股票价格偏离：若某池边际报价为 r 个 Meme/1 个股票代币，Meme 的独立美元价为 M，股票代币的独立链上美元价为 T，则隐含股票价 `I = r × M`，偏离为 `100 × (I / T - 1)`。要求单位、decimals、方向一致，且 M/T 不能由同一个目标池循环反推。缺独立报价时不计算。

上述为本规划对文档模糊定义的修订建议。两项都不是扣除手续费、滑点后的可执行套利收益。归一化走势图用于比较变化，不是价差本身，也不是相关性的证明。

### 5.5 其他指标

- Stock Flow：建议定义为已验证池中股票侧 LP 加入减退出，按事件时价格估值；普通交换引起的储备变化和余额差不算 LP 净流入。缺 LP 事件时保持未就绪。
- 热度：版本化保存输入、窗口、归一化和权重；完整持有人增速、成交量增速与已定义股票资产规模尚未齐备时，继续使用独立命名的交易活动指标。
- 捕获率：分子必须是同链、同时点、同覆盖池集合分母的子集。只有 Top N 池就标“已覆盖池捕获率”；缺分母不发布全链指标。
- 相关性：独立价格、统一采样与充足共同样本；区分价格水平相关与收益率相关，休市或恒定序列不强算，不能称为因果。
- 行业篮子：保留五类业务目录、固定基期与成分版本，优先恢复已有芯片篮子连续采样；其他行业只在真实合格成分达到门槛后发布。过期显示最后有效指数及时间，新增区间留缺口；调仓记录版本并保持连续性，不能随着列表变化重设基期。

## 6. 实施顺序与交付门槛

以下为按现有基础估算的 8–12 个有效开发日，外部接口受限不计为已完成。两份文档中的 9/21 内评、9/25 提交可作为候选检查点，具体赛程不作为本轮已核实的外部事实。

| 阶段 | 建议工作量 | 具体交付 | 通过后才能推进的门槛 |
|---|---|---|---|
| P0-A：可信数据基础 | 2–3 日 | 字段字典、来源能力矩阵、链+CA 迁移、成交范围拆分、采集预算与优先队列、历史查询 | 不混合场内与 DEX 数字；旧详情可查；重点资产不被目录任务长期挤占 |
| P0-B：用户使用闭环 | 3–4 日 | 新导航、总览基础版、股票三级视图、独立配对分析、统一筛选路由、证据优先详情、核心 tooltip/双语/头像 | 同一实际对象从总览一路到证据/配对/外部入口身份一致；空态有原因 |
| P1：覆盖与差异化 | 2–3 日 | 根据能力实测扩展 BNB/Robinhood 的关系采集；合格样本价差/趋势；Top 10 与 LP 结构；篮子连续性 | 新来源真实 CA/池/成交抽样对账；计算可复算，缺口如实展示 |
| 发布验证 | 1–2 日 | 故障、重启、历史、语言、移动端、域名子路径验证；备份与回滚包；演示样本说明 | 核心验收全通过，外部依赖未通过则展示明确覆盖状态，不宣称完成 |
| P2：后续 | 单独评估 | 完整 LP 资金流/热度/捕获率、AI 引用式叙事、Trigger 图、大额地址历史 | 先取得必要输入和真实样本，再排期；高级聚类、交易执行和准确收益预测不纳入本轮 |

若以 9/21 做内部演示，范围优先冻结为 P0 数据基础和页面闭环，P1 按真实接口完成情况加入。不得为赶日期伪造三链关系样本或历史曲线。基础图标头像与 Trigger 梗图是两项需求，前者随 P0 UI，后者保留 P2。

## 7. 验收清单

1. 抽取至少 10 个不同来源、不同状态的资产，对账值、单位、来源、时间和窗口；场内成交不加入链上合计，同源重复观察不重复计数。
2. 相同 CA 在不同链、相同名称在多个 CA、同 ticker 不同标的分别保留；同股票多部署可聚合查看，但关系只能落到真实部署。
3. 服务重启、重复事件重放、分页超过 100 条后，旧事件/详情继续可查，成交不重复；迁移不丢原有历史。
4. 模拟上游缺失/限流/失败：不变成 0，不刷新旧字段观察时间；缓存可读，预算与退避生效。
5. 至少观测三个采集周期：记录总请求数、重点资产年龄和排队情况；浏览器刷新与语言操作不增加上游调用。
6. 总览计数点击到列表后可核对；统计先去重再聚合；列表分页和五组筛选/排序组合结果正确，刷新 URL 状态保留。
7. 股票代码、股票 CA、Meme、池地址、复制、外部入口逐个验证；旧链接、浏览器前进后退和 `/dashboard/` 深链接可用。
8. 两类价差在具备独立报价的样本人工复算；缺换算比例、价格为零、不同时间、休市、样本不足都产生正确状态。
9. 篮子成分暂时过期不删除成员；恢复后继续原基期；没有合格成分的行业不出示例指数。
10. 中英文切换操作立即有反馈，目标 200ms 内更新操作状态，已有可见文案目标 500ms 内完成切换（正常测试环境，不等待网络）；导航、图表、tooltip、失败提示同步，滚动/筛选保留。
11. 手机宽度下搜索、筛选、复制、tooltip 和配对选择可用；图片失败有占位，图表未就绪有原因；空白数据不能占据整个详情首屏。
12. 真实市场无新事件时显示最新检查时间；不得用“必须出现新记录”的验收诱导生成假事件。能力不足或外部跳转不支持时如实标记。

## 8. 三文档需求追踪

| 迭代清单 | 正式 PRD | 演示 PRD | 本规划落点 |
|---|---|---|---|
| U2-1 | A1 | FR-1 | 字段状态、5 分钟预算采集、对账与持久化 |
| U2-2 | A2 / D1 | FR-2 | 全量排序筛选、URL 状态、Meme 主表 |
| U2-3 | A5 | FR-3 | 统一身份与路由、外部资产入口 |
| U2-4 | A2 / D1 / E1 | FR-4 | 全源默认、多链实体键与能力矩阵 |
| U2-5 | I1 | FR-5 | 来源面板准备状态；公开事实核实后展示 |
| U2-6 | B1–B3 / C1 | FR-6 | 总览、实时事件子区和可对账分布 |
| U2-7 | F1 | FR-7 | 有条件的双价差、统一时间基准 |
| U2-8 | A3 | FR-11 | 双语术语字典、桌面/移动端说明 |
| U2-9 | G1 | FR-8 | 股票部署列表+选中池分析的独立页面 |
| U2-10 | H1 / H2 | FR-9 | 持仓与 LP 结构分开、池级范围、费率非收益率 |
| U2-11 | E1 / E2 | FR-10 | 标的→部署→Meme，多来源同构展示 |
| U2-12 | A4 | FR-11 | 即时语言切换与完整界面适配 |
| U2-13 | F2 | FR-12 | 元数据头像随主流程；有出处的 Trigger 图后续 |

## 9. 开工前的技术验证任务

不额外要求用户重复确认已有产品选择。首阶段完成：核实账户额度与各接口单次成本；以真实合约检查各链元数据、行情、成交、池和持仓能力；验证两类价差的独立价格与换算比例；对现有数据库制作可恢复备份并演练迁移；核实外部交易链接与 Arc 公告后才发布相关文案。

若缺少某项外部能力，只调整该项交付范围并报告缺口；页面、数据字典、历史保障和已有链的核心闭环继续推进。新增付费服务或更高请求频率不默认包含在本规划中。
